<nav class="navbar navbar-inverse" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to 'Come2eat', restaurant_foods_path, class: 'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if current_restaurant.blank? %>
          <li><%= link_to("Sign Up", new_restaurant_registration_path) %></li>
          <li><%= link_to("Log In", new_restaurant_session_path) %></li>
        <% else %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Hi!, <%= current_restaurant.email %>
              <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li> <%= link_to("帳號設定", edit_restaurant_registration_path )%></li>
              <li> <%= link_to("登出", destroy_restaurant_session_path, method: :delete) %></li>
            </ul>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>